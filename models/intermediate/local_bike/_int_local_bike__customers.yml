version: 2

models:
  - name: int_local_bike__customers
    description: '{{ doc("int_local_bike__customers") }}'
    columns:
      - name: customer_id
        description: "Primary key, unique identifier for each customer."
        tests:
            - unique
            - not_null